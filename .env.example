# =============================================================================
# AUTENTICAÇÃO
# =============================================================================

# Usuário e senha para acesso ao sistema
AUTH_USERNAME=admin
AUTH_PASSWORD=admin123

# =============================================================================
# CONFIGURAÇÃO DO BANCO DE DADOS SQL SERVER
# =============================================================================

# Servidor SQL Server (incluindo instância se necessário)
# Exemplo: localhost\SQLEXPRESS22 ou THALIKPC\SQLEXPRESS22
DB_SERVER=your_server\your_instance

# Nome do banco de dados
DB_DATABASE=your_database_name

# Usuário do SQL Server
DB_USER=your_username

# Senha do SQL Server
DB_PASSWORD=your_password

# Porta do SQL Server (padrão: 1433)
# Se usar instância nomeada, pode não precisar especificar
DB_PORT=1433

# Encriptação da conexão (true/false)
# Para desenvolvimento local, geralmente é false
DB_ENCRYPT=false

# Confiar no certificado do servidor (true/false)
# Para desenvolvimento local com SQL Server Express, geralmente é true
DB_TRUST_SERVER_CERTIFICATE=true

# Timeout de conexão em milissegundos (padrão: 15000)
DB_CONNECTION_TIMEOUT=15000

# Timeout de requisição em milissegundos (padrão: 15000)
DB_REQUEST_TIMEOUT=15000

# =============================================================================
# CONFIGURAÇÃO SMTP (Para envio de e-mails - Implementação Futura)
# =============================================================================

# Servidor SMTP
SMTP_HOST=smtp.example.com

# Porta SMTP (587 para TLS, 465 para SSL, 25 sem criptografia)
SMTP_PORT=587

# Usar TLS/SSL (true/false)
SMTP_SECURE=false

# Usuário para autenticação SMTP
SMTP_USER=your_email@example.com

# Senha para autenticação SMTP
SMTP_PASSWORD=your_smtp_password

# E-mail do remetente
SMTP_FROM_EMAIL=noreply@soautomacao.com.br

# Nome do remetente
SMTP_FROM_NAME=Empresa Genérica - Sistema de Cobranças

# =============================================================================
# CONFIGURAÇÃO DA APLICAÇÃO
# =============================================================================

# Ambiente da aplicação (development, production, test)
NODE_ENV=development

# URL base da aplicação (para links em e-mails, etc)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# CONFIGURAÇÃO DE LOGGING (Opcional)
# =============================================================================

# Nível de log (error, warn, info, debug)
LOG_LEVEL=info

# Habilitar log de queries SQL (true/false)
# ATENÇÃO: Pode expor dados sensíveis, usar apenas em desenvolvimento
LOG_SQL_QUERIES=false
